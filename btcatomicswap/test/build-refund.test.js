import {refund} from '../src/common/build-refund';
import {buildContract} from '../src/contract/build-contract.js'

const assert = require('assert');

describe('#refund()', function () {
  it('should create refundTx', async () => {
    // refund(strContract, strContractTx, strSecret)
    // refund(testData6.contract, testData6.contractTx, testData6.secret)

  });
});
